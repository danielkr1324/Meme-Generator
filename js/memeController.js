'use strict'

let gCtx
let gElCanvas
let isDrag = false

let gElCurrImage

const TOUCH_EVENTS = ['touchstart', 'touchmove', 'touchend']

function renderMeme(meme) {
  let image = new Image()
}
